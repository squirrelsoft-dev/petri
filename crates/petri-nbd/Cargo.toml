[package]
name = "petri-nbd"
version = "0.1.0"
edition = "2024"
description = "Layered block-device backend for Petri VM images (NBD)"
license = "MIT"

[dependencies]
# SHA-256 for content-addressed layer identity (§8.1), matching Petri's
# existing SHA256SUMS image-integrity convention.
sha2 = "0.10"
