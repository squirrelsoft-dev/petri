[package]
name = "petri-guest"
version = "0.1.0"
edition = "2024"
description = "Petri guest agent skeleton"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.9"

