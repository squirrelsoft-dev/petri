[package]
name = "petri-protocol"
version = "0.1.0"
edition = "2024"
description = "Shared Petri protocol wire types"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.9"
