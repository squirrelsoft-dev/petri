[package]
name = "petri"
version = "0.1.0"
edition = "2024"
description = "Petri host CLI and library skeleton"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
